#include <iostream>
#include <string>
using namespace std;

int main() {
    string text = "programming";
    string reversed = "";
    
    for (int i = text.length() - 1; i >= 0; i--) {
        reversed += text[i];
    }
    
    cout << reversed;
    return 0;
}
