#include <iostream>
using namespace std;

int main() {
    char uppercase = 'Z';
    char lowercase;
    
    if (uppercase >= 'A' && uppercase <= 'Z') {
        lowercase = uppercase + 32;
    } else {
        lowercase = uppercase;
    }
    
    cout << lowercase;
    return 0;
}
